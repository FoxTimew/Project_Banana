using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StartTransUwU : MonoBehaviour
{
    public DisableCanvas canvas;
    // Start is called before the first frame update
    void Start()
    {
        canvas.TPTransition();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
